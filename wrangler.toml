# Cloudflare Pages Configuration for LiveTrackings
# This is a static site with HTML files in the root directory

name = "livetrackings"
compatibility_date = "2024-11-23"

# This configuration is for Pages static site hosting
# The HTML files (index.html, blog.html, etc.) are in the root directory
# No build process is needed - Pages will serve them directly

# Uncomment below if you need API routes with Workers
# [env.production]
# routes = [
#   { pattern = "livetrackings.com/api/*", zone_name = "livetrackings.com" },
#   { pattern = "www.livetrackings.com/api/*", zone_name = "livetrackings.com" }
# ]
